name: modernlogintute
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: '>=2.18.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  firebase_auth: ^5.1.3
  firebase_core: ^3.3.0
  google_sign_in: ^6.2.1
  sign_in_with_apple: ^6.1.1
  shared_preferences: ^2.3.1
  connectivity_plus: ^4.0.1
  battery_plus: ^3.0.0
  fluttertoast: ^8.0.8
  audioplayers: ^0.20.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true


  assets:
    - assets/notification-9-158194.mp3
    - lib/images/
